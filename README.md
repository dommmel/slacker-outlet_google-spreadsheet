## Installation
run the ``install`` script to 

1. install all dependencies and 
2. setup google api authorization

## Usage
make sure you run the install script at least once and the outlet is activated